{
  "name": "@sportos/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json --pretty false",
    "typecheck": "tsc -b tsconfig.json --pretty false",
    "test": "vitest run --passWithNoTests",
    "test:integration": "vitest run src/repositories/import-jobs.repository.integration.test.ts src/repositories/rule-changes.repository.integration.test.ts src/repositories/canonical-export.repository.integration.test.ts --no-file-parallelism"
  },
  "dependencies": {
    "@sportos/domain": "workspace:*",
    "@sportos/shared": "workspace:*",
    "kysely": "^0.27.5",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  }
}
